<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p>Completed:<%= @task.completed %></p>

<p><%= link_to "Update the task", edit_path(@task.id) %></p>


<p><%= link_to "Delete",
  destroy_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></p>
